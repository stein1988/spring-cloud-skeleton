# 开发习惯规则

## 后端服务管理

**不要自动启动或重启后端服务（Spring Boot 应用）**。

- 后端服务（module-user）通常已在 IDE 中运行或处于 Debug 模式。
- 除非用户在对话中明确要求启动、重启或调试后端，否则不要执行 `mvn spring-boot:run`、`java -jar` 等命令。
- 当前端代码修改完成后，直接提示用户刷新浏览器页面即可。
